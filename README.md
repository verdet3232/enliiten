# Enliten Investor Walkthrough
A nine-scene, single-file web walkthrough of the Enliten Luminz concept — open `index.html` directly or serve the folder (GitHub Pages ready; `404.html` mirrors `index.html` for hash-route safety).
To update a scene, edit its `<section class="scene">` block in `index.html`; keep the mandated verbatim strings intact (hierarchy sentences, vetting chain, rate formula, the 9% quote) and stay inside each scene's word budget.
Drop the official `enliten-logo.png` into `assets/` — the page auto-detects it and swaps the cover fallback mark for the light logo plate.
Ryan Shook · Shook Solutions · 07-01-2026
